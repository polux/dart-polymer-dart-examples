import 'package:polymer/component_build.dart';
import 'dart:io';

// Testing: this should eventually produce warnings in the editor
// when I do something wrong.

main() {
  build(new Options().arguments, ['index.html']);
}